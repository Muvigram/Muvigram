<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/fragment_camera"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <SurfaceView
        android:id="@+id/camera_surface_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <ImageButton
        android:id="@+id/camera_camera_shutter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="30dp"
        android:src="@drawable/ic_camera_shutter_60dp"
        android:background="@color/transparent"
        android:contentDescription="@string/camera_record_video"/>

    <ImageButton
        android:id="@+id/camera_switch_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_margin="45dp"
        android:src="@drawable/ic_camera_switch_view_30dp"
        android:background="@color/transparent"
        android:contentDescription="@string/camera_change_mode"/>

    <ImageButton
        android:id="@+id/camera_flash_control"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_margin="45dp"
        android:src="@drawable/ic_camera_flash_on_30dp"
        android:background="@color/transparent"
        android:contentDescription="@string/camera_change_mode"
        />

</RelativeLayout>
